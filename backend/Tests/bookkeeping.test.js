require("dotenv").config();

const bookkeeping = require("../src/services/bookkeepingServices");

describe("Bookkeeping Tests", () => {
    test("should add recipt to db", async () => {

    }
    )});